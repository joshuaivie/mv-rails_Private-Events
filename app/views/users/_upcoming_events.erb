<% if @user.attended_events.where("Date >= ?", Time.now).count > 0 %>
  <%= show_events(@user.attended_events.where("Date >= ?", Time.now), {:event_state => "upcoming_event", :display_type => "list"})%>
<% else %>
<% end %>
